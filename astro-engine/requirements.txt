# Kept in sync with backend/requirements.txt (the copy Docker actually
# builds from) — this root copy exists only for host auto-detection on
# Render/Railway/Heroku-style buildpacks. See backend/requirements.txt for
# why these are pinned rather than floating >=.
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic[email]==2.13.5
jinja2==3.1.6
pyswisseph==2.10.3.2
weasyprint==70.0
geoip2fast==1.2.2
stripe==15.6.1
